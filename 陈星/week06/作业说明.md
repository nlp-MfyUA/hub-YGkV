# 第六周作业说明

- **学生**：陈星 (ppaul-s)
- **课程目录**：`D:\BaiduNetdiskDownload\第6周：RAG工程化实现\Week06`
- **本地 ES**：Elasticsearch 8.17.4（`D:\es\elasticsearch-8.17.4`，已装 IK，`http://127.0.0.1:9200`）
- **向量模型**：课程包 `..\models\BAAI\bge-small-zh-v1.5`（512 维）

作业要求：1）本地配置 ES，完成全文检索、条件过滤、向量检索并截图；2）跑通 RAG 01–08 并截图。

---

## 作业 1：Elasticsearch 三类检索

在课程 `Week06` 目录下运行 `04_ES测试.py` ~ `07_ES向量检索.py`。本仓库附带同名脚本与运行截图。

### 1. 连接与 IK 分词（`04_ES测试.py`）

- 集群 `rag-cluster`，版本 8.17.4，连接成功
- `ik_smart` / `ik_max_word` 对中文「我在使用 Elasticsearch，这是我的测试。」正常出词
- 截图：`截图/01_ES连接与IK分词.png`

### 2. 全文检索（`05_ES基础.py`）

- 索引 `blog_posts_py`，title/content 使用 `ik_max_word` + `ik_smart`
- Query：标题 `match "入门指南"` → 命中《Elasticsearch 入门指南》，BM25 **1.6575**
- Query：正文 `match "搜索技术"` + `filter.term author=张三` → 同一篇，得分 **1.4114**
- 截图：`截图/02_全文检索.png`

### 3. 条件过滤（`06_ES进阶.py`）

- 索引 `products`
- 全文：`multi_match "智能"` → 智能手机 A001，得分 **0.6549**
- 过滤：`category=电子产品` AND `on_sale=true` AND `price<1000` → 无线蓝牙耳机 B002（4999 的手机被价格条件挡掉）
- 截图：`截图/03_条件过滤.png`

### 4. 向量检索（`07_ES向量检索.py`）

- 索引 `semantic_search_demo`，`dense_vector` 512 维 cosine
- Query：`关于AI和未来的技术`

| 排名 | cosine | 内容 |
| --- | ---: | --- |
| 1 | **0.8596** | 人工智能是未来的趋势。 |
| 2 | 0.7824 | 自然语言处理技术让机器理解人类语言。 |
| 3 | 0.7792 | 机器学习是人工智能的一个重要分支。 |

天气 / 篮球等无关句子未进入 Top3。截图：`截图/04_向量检索.png`

---

## 作业 2：跑通 RAG 01–08

均在课程 `Week06` 目录执行，知识库为 `汽车知识手册.pdf`（354 页），问题集 `questions.json`（301 条）。

| 脚本 | 作用 | 证据 |
| --- | --- | --- |
| RAG101_01 | 读问题 + 解析 PDF | `截图/05_RAG01读取数据集.png` |
| RAG101_02 | TFIDF 检索 | `submit_tfidf_retrieval_top1.json` → page_117 |
| RAG101_03 | BM25 检索 | `submit_bm25_retrieval_top1.json` → page_116 |
| RAG101_04 | BGE 整页语义检索 | `submit_bge_retrieval_top1.json` → page_116 |
| RAG101_05 | BGE 切片语义检索 | `submit_bge_sgement_retrieval_top1.json` → page_4 |
| RAG101_06 | BGE+BM25 融合后 rerank | `submit_fusion_bge+bm25_rerank_retrieval.json` → page_115 |
| RAG101_07 | RRF 多路召回 | `submit_fusion_bge+bm25_retrieval.json` → page_116 |
| RAG101_08 | 检索结果送大模型问答 | 依赖智谱/第三方 Chat API，**未把密钥提交进仓库** |

示例问题「前排座椅通风」各路 Top1 对照见 `截图/06_RAG检索结果汇总.png`。json 仍留在课程目录，未整包提交（手册 PDF 15MB，不进作业仓库）。

---

## 运行方式

```bat
cd /d D:\BaiduNetdiskDownload\第6周：RAG工程化实现\Week06
set PYTHONUTF8=1
D:\miniconda3\envs\py312\python.exe 04_ES测试.py
D:\miniconda3\envs\py312\python.exe 05_ES基础.py
D:\miniconda3\envs\py312\python.exe 06_ES进阶.py
D:\miniconda3\envs\py312\python.exe 07_ES向量检索.py
D:\miniconda3\envs\py312\python.exe RAG101_01_读取数据集.py
```

ES 需先启动：`D:\es\elasticsearch-8.17.4\bin\elasticsearch.bat`
