# 作业四：运算符综合应用
# ============================================================================
#
# 任务描述：
# 编写一个程序，实现以下功能：
# 1. 定义两个整数变量 a = 15, b = 4
# 2. 计算并输出以下运算结果：
#    - a + b（加法）
#    - a - b（减法）
#    - a * b（乘法）
#    - a / b（除法）
#    - a // b（整除）
#    - a % b（取余）
#    - a ** b（幂运算）
# 3. 使用比较运算符比较 a 和 b 的大小关系，输出：
#    - a > b 的结果
#    - a < b 的结果
#    - a == b 的结果
#    - a != b 的结果
# 4. 使用逻辑运算符，判断以下条件，并输出结果：
#    - a > 10 and b > 2
#    - a > 20 or b < 5
#    - not (a < b)
#
# 要求：
# - 每个运算都要有清晰的说明
# - 使用格式化字符串输出
# - 添加适当的注释


# 先完成基本功能

def calculate(a, b):
    # 加法计算
    print(f"加法计算:a + b = {a + b}" )

    #减法计算
    print(f"减法计算：a - b = {a - b}")

    #乘法计算
    print(f"乘法运算：a * b = {a * b}")

    # 除法计算
    print(f"除法计算:a / b = {a / b}" )

    #整除计算，整数位
    print(f"整除计算：a // b = {a // b}")

    #取余计算 余数
    print(f"取余运算：a % b = {a % b}")

    #幂运算计算
    print(f"幂运算运算：a ** b = {a ** b}")


    # 大于
    print(f"a > b = {a > b}")

     # 小于
    print(f"a < b = {a < b}")

     # 比较
    print(f"a == b = {a == b}")

     # 不等于
    print(f"a != b = {a != b}")


    print(f"a > 10 and b > 2 : {a > 10 and b > 2}")

    print(f"a > 20 or b < 5 : {a > 20 or b < 5}")

    #取反
    print(f"not (a < b) : {not (a < b)}")


if __name__ == "__main__":
    calculate(15,4)

