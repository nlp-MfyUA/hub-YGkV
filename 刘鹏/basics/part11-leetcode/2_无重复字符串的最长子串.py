"""
LeetCode 3. 无重复字符的最长子串 (Longest Substring Without Repeating Characters)

题目描述：
给定一个字符串 s ，请你找出其中不含有重复字符的最长子串的长度。

示例 1：
输入: s = "abcabcbb"
输出: 3
解释: 因为无重复字符的最长子串是 "abc"，所以其长度为 3。

示例 2：
输入: s = "bbbbb"
输出: 1
解释: 因为无重复字符的最长子串是 "b"，所以其长度为 1。

示例 3：
输入: s = "pwwkew"
输出: 3
解释: 因为无重复字符的最长子串是 "wke"，所以其长度为 3。
     请注意，你的答案必须是子串的长度，"pwke" 是一个子序列，不是子串。

提示：
- 0 <= s.length <= 5 * 10^4
- s 由英文字母、数字、符号和空格组成

解题思路：
滑动窗口法：
1. 使用两个指针 left 和 right 表示窗口的左右边界
2. 使用哈希表（字典）记录字符最后出现的位置
3. 当遇到重复字符时，移动左指针到重复字符的下一位
4. 不断更新最大长度
时间复杂度：O(n)
空间复杂度：O(min(m, n))，m是字符集大小
"""
def length_of_longest_substring(s: str) -> str:
    if not s:
        return 0
    
    # 使用字典记录字符最后的位置，key为具体值，value为索引值
    
